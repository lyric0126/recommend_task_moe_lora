search:
  seed: 202904
  num_trials: 60
  dev_anchor_count: 1200
  candidate_pool_multiplier: 2
  output_dir: data/processed/search
  min_pseudo_user_count: 1000
  min_strict_count: 500
  interaction_cap_per_source_user: 80
  search_ranges:
    weights:
      semantic: [0.05, 0.25]
      recency_semantic: [0.00, 0.10]
      activity: [0.15, 0.40]
      temporal: [0.20, 0.45]
      behavior: [0.10, 0.30]
    strict_threshold: [0.65, 0.85]
    medium_threshold: [0.45, 0.70]
    top_k: [1, 2, 3, 5]
    fallback_candidate_size: [50, 100, 180, 300]
base_config: configs/pseudo_user_pipeline_v2fix.yaml
